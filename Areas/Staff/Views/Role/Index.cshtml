@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>
@{
    ViewData["Title"] = "List Role ";
    Layout = "_Layout";
}

<!-- Page Banner Start -->
@await Html.PartialAsync("_PageBannerStart")
<!-- Page Banner End -->

<h2>@ViewData["Title"]</h2>

<div class="container">
    <div class="row justify-content-center">
        <div class="mb-2">
            <a class="btn btn-secondary" asp-area="Staff" asp-controller="User" asp-action="Index">Manage Member</a>
        </div>

        <table class="table">
            <tr>
                <th>Role</th>
            </tr>

            @foreach (var role in new List<string> { "Tutor", "Staff", "Student" })
            {
                <tr>
                    <td>@role</td>
                    @* <td>
                        <form asp-controller="Role" asp-action="Delete" asp-route-name="@role" method="post" onsubmit="return confirm('Are you sure you want to delete this role?');">
                            <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                        </form>
                    </td> *@
                </tr>
            }
        </table>
    </div>
</div>
